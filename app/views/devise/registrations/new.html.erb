<h2>Add Staff Members To Your School</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <p>Add the emails of any staff members you would like to include below.  They will be sent an e-mail with a pre-generate password that they can change.

  <table>
    <th>
      <td>E-Mail Address</td>
      <td>Admin?</td>
    </th>
    <tr>
      <%= f.email_field :email %>
    </tr>
      <%= check_box_tag(:role, 'admin') %>
    </tr>

    <%= submit_tag("Submit") %>
<% end %>

<!-- # <%# render "StaffMembers/shared/links" %> -->
